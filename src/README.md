The symbolic links from here to the `../site/` directory are for `elm reactor`. Linked directories:

* images -> ../site/images
* logo-maker -> ../site/logo-maker
* page -> ../site/page
* template -> ../site/template

